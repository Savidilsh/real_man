# Checkpoints

Do not commit model checkpoint binaries here.

Expected Mosaic3D checkpoint:

```text
spunet34c.ckpt
```

Recommended local path on this machine:

```text
/home/dongan/ws_demo/Mosaic3D/ckpt_raw/converted/spunet34c.ckpt
```

Pass it to the pipeline with:

```bash
--ckpt /home/dongan/ws_demo/Mosaic3D/ckpt_raw/converted/spunet34c.ckpt
```

