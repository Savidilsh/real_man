# Data

Put input point clouds outside git, or mount them by absolute path.

Supported inputs include:

- `.ply`
- `.las` / `.laz`
- CloudCompare `.bin`
- `.npy` with shape `[N, 3]` or `[N, 6]`

Large point clouds should not be committed to this repository.

